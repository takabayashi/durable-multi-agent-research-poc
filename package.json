{
  "name": "durable-multi-agent-research-poc",
  "version": "0.1.0",
  "private": true,
  "description": "Durable multi-agent research assistant built on Restate (proof of concept).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch --env-file-if-exists=.env src/app.ts",
    "start": "node --env-file-if-exists=.env dist/app.js",
    "cli": "tsx --env-file-if-exists=.env src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:ci": "biome ci .",
    "format": "biome check --write .",
    "check": "npm run lint:ci && npm run typecheck && npm run build && npm test"
  },
  "license": "MIT",
  "dependencies": {
    "@restatedev/restate-sdk": "^1.14.5",
    "@restatedev/restate-sdk-clients": "^1.14.5",
    "openai": "^6.42.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^25.9.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
